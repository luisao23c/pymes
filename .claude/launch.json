{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "catamanager",
      "runtimeExecutable": "powershell",
      "runtimeArgs": [
        "-ExecutionPolicy", "Bypass",
        "-Command", "& '.\\tools\\start-mysql.ps1'; if ($?) { node server.js } else { exit 1 }"
      ],
      "port": 3000
    }
  ]
}
